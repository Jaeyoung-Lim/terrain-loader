version: '3.8'

services:
  terrain-downloader:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./downloads:/app/downloads
    environment:
      - FLASK_ENV=development
    restart: unless-stopped
